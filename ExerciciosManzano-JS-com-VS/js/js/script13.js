let numero = parseInt(prompt("Digite um número: "))
let contadora = 1

for(contadora; contadora<=10; contadora++){
    console.log(`o numero ${numero} * ${contadora} = ` + numero * contadora)
}
